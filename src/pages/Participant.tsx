import Stand from "./Stand";

const Participant: React.FC = () => {
  return <Stand role="Participant" isAdmin={false} standName="" animatorStandSetted={false} />;
};

export default Participant;
