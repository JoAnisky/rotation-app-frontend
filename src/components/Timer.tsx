import { Box } from "@mui/material";

const Timer: React.FC = () => {
  return (
    <Box className="timer-container">
      <div>
        <p>Equipe NOM</p>
        <p>Stand NOM</p>
      </div>
    </Box>
  );
};

export default Timer;
